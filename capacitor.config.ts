import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.doable.frontend',
  appName: 'doable-frontend',
  webDir: 'dist/doable-frontend/browser'
};

export default config;
